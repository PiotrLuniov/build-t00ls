apiVersion: v1
kind: Service
metadata:
  name: sonarqube-service
  namespace: jenkins
  labels:
    app: sonarqube
spec:
  selector:
    app: sonarqube
  type: ClusterIP
  ports:
  - name: sonarqube-svc
    protocol: TCP
    port: 9000
    targetPort: sonarqube-port
